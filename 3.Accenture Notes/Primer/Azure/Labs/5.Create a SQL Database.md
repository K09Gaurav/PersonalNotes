#cloud #accenture #quiz #azure

# Azure Lab: Create a SQL Database

## Lab Objective

In this activity, you will:

- Create an Azure SQL Database
- Create a SQL Server
- Load the AdventureWorksLT sample database
- Configure networking and security settings
- Run SQL queries using Query Editor

---

# Important Note

After completing the lab:

1. Click **Evaluate**.
2. Verify that you receive **100 marks**.
3. If required, click **Evaluate** multiple times.
4. Only click **Submit Attempt** after achieving **100 marks**.

---

# Steps to Connect in Azure Portal

1. Click **Start Attempt** and wait for the environment provisioning.
2. Azure credentials will be provided.
3. Click **Generate OTP**.
4. Copy the OTP and sign in to Azure Portal.
5. Complete the lab activity.
6. Click **Submit Attempt** after receiving full marks.

---

# Overview

Azure SQL Database is a fully managed relational database service based on Microsoft SQL Server.

In this lab, you will:

```text
Create SQL Server
        │
        ▼
Create SQL Database (db1)
        │
        ▼
Load AdventureWorksLT Sample Data
        │
        ▼
Run SQL Queries
```

---

# Task 1: Create the Database

## Step 1: Sign In

Open Azure Portal:

```text
https://portal.azure.com
```

---

## Step 2: Open SQL Databases

Navigate to:

```text
All Services
→ SQL Databases
→ + Add
→ + Create
→ + New
```

---

## Step 3: Configure the Basics Tab

### Database Settings

| Setting | Value |
|----------|--------|
| Subscription | Use default supplied |
| Resource Group | Use default supplied |
| Database Name | db1 |

---

### Create a New SQL Server

Click:

```text
Server
→ Create New
```

Configure the following:

| Setting | Value |
|----------|--------|
| Server Name | sqlserverxxxx |
| Server Admin Login | sqluser |
| Password | Pa$$w0rd1234 |
| Location | Nearest Indian Region |

Click:

```text
OK
```

> Replace `xxxx` with unique letters or numbers because the server name must be globally unique.

### Example

```text
sqlserver1234
sqlserver2025
sqlserverabc1
```

---

## Step 4: Configure Database Settings

| Setting | Value |
|----------|--------|
| Want to Use SQL Pool? | No |
| Workload Environment | Development |

![Pasted image 20260602165630](../../images/Pasted%20image%2020260602165630.png)

---

## Step 5: Configure Compute and Storage

Click:

```text
Compute + Storage
→ Configure Database
```

Select:

| Setting | Value |
|----------|--------|
| Service Tier | Basic (For Less Demanding Workloads) |

Click:

```text
Apply
```

---

## Step 6: Configure Backup Storage

| Setting | Value |
|----------|--------|
| Backup Storage Redundancy | Locally Redundant Backup Storage |

---

# Networking Configuration

Open:

```text
Networking Tab
```

Configure the following:

| Setting | Value |
|----------|--------|
| Connectivity Method | Public Endpoint |
| Allow Azure Services and Resources to Access this Server | Yes |
| Add Current Client IP Address | No |

Leave all remaining settings as default.

![Pasted image 20260602165657](../../images/Pasted%20image%2020260602165657.png)

---

# Security Configuration

Open:

```text
Security Tab
```

Configure:

| Setting | Value |
|----------|--------|
| Azure Defender for SQL | Not Now |

---

# Additional Settings Configuration

Open:

```text
Additional Settings Tab
```

Configure:

| Setting | Value |
|----------|--------|
| Use Existing Data | Sample |

This loads the:

```text
AdventureWorksLT
```

sample database.

![Pasted image 20260602165711](../../images/Pasted%20image%2020260602165711.png)

---

# Create the Database

1. Click **Review + Create**.
2. Review all settings.
3. Click **Create**.

Deployment time:

```text
2–5 Minutes
```

Resources created:

- Resource Group
- SQL Server
- SQL Database (db1)

---

# Task 2: Test the Database

## Step 1: Open the Database

After deployment:

```text
Deployment Complete
→ Go To Resource
```

OR

```text
All Resources
→ SQL Databases
→ db1
```

Refresh if the database is not immediately visible.
![Pasted image 20260602165726](../../images/Pasted%20image%2020260602165726.png)

---

## Step 2: Open Query Editor

Navigate to:

```text
db1
→ Query Editor (Preview)
```

---

## Step 3: Log In

Use the credentials:

| Setting | Value |
|----------|--------|
| Username | sqluser |
| Password | Pa$$w0rd1234 |

![Pasted image 20260602165738](../../images/Pasted%20image%2020260602165738.png)

### Firewall Note

If access is blocked:

```text
Whitelist Current IP Address
```

and retry login.

---

## Step 4: Run the Query

Paste the following SQL query:

```sql
SELECT TOP 20
    pc.Name AS CategoryName,
    p.Name AS ProductName
FROM SalesLT.ProductCategory pc
JOIN SalesLT.Product p
ON pc.ProductCategoryID = p.ProductCategoryID;
```


![Pasted image 20260602165800](../../images/Pasted%20image%2020260602165800.png)

---

## Step 5: Execute the Query

Click:

```text
Run
```

---

## Expected Result

The Results pane should display:

![Pasted image 20260602165840](../../images/Pasted%20image%2020260602165840.png)

| CategoryName | ProductName |
|-------------|-------------|
| Example Category | Example Product |
| Example Category | Example Product |
| ... | ... |

The output contains:

- Product Categories
- Product Names
- Data from AdventureWorksLT sample database

---

# Query Explanation

```sql
SELECT TOP 20
```

Returns the first 20 rows.

```sql
FROM SalesLT.ProductCategory
```

Retrieves category information.

```sql
JOIN SalesLT.Product
```

Joins product details.

```sql
ON ProductCategoryID
```

Matches products to categories.

---

# Architecture Overview

```text
Azure SQL Server
        │
        ▼
Database (db1)
        │
        ▼
AdventureWorksLT Sample Data
        │
        ▼
Query Editor
        │
        ▼
SQL Results
```

---

# Lab Summary

You successfully:

- Created an Azure SQL Server.
- Created a SQL Database named **db1**.
- Configured networking and security settings.
- Loaded the AdventureWorksLT sample database.
- Connected using Query Editor.
- Executed SQL queries.
- Retrieved product and category information.

---

# Key Configuration

| Component | Value |
|------------|--------|
| Database Name | db1 |
| Server Name | sqlserverxxxx |
| Admin Login | sqluser |
| Password | Pa$$w0rd1234 |
| Service Tier | Basic |
| Connectivity | Public Endpoint |
| Sample Data | AdventureWorksLT |
| Backup Redundancy | Locally Redundant |

---

# Outcome

The Azure SQL Database is successfully deployed and populated with AdventureWorksLT sample data. You can now use Query Editor to run SQL queries, explore relational data, and test database functionality directly from the Azure Portal.